import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function NightModeSettings() {
  return (
    <View>
      <Text>NightModeSettings</Text>
    </View>
  )
}

const styles = StyleSheet.create({})